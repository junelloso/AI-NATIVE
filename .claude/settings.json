{
  "mcpServers": {
    "obsidian-docs": {
      "command": "mcp-server-filesystem",
      "args": [
        "/home/user/AI-NATIVE/vault"
      ],
      "description": "AI-NATIVE Obsidian documentation vault — architecture, ADRs, designs, runbooks"
    },
    "project-code": {
      "command": "mcp-server-filesystem",
      "args": [
        "/home/user/AI-NATIVE"
      ],
      "description": "AI-NATIVE source code repository root"
    }
  },
  "notes": {
    "postgres": "Add when PostgreSQL is created (Phase 1) — see settings.local.json.example",
    "github": "Add when GitHub org is set up (Phase 1) — see settings.local.json.example"
  }
}
